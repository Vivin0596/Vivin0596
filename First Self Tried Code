#  To print the Winners name from first to last with the timings

# Winners names in order.
a =['vishnu', 'bala', 'harish', 'joshi']

# Winners race completion timings.
b =[11.2,17.2,15,12]

c= []
c= list(b)
c.sort()
z = 1
for i in range(len(c)):
    for j in b:
        if c[i]==j:
            print(a[b.index(j)] , 'completed',z,'place with the timing of :',c[i])
            z+=1
